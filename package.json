{
  "name": "cropview-v2",
  "version": "2.0.0",
  "description": "CropView v2 — single-file PWA for geo-tagged field crop capture with corrected directionality",
  "main": "index.html",
  "scripts": {
    "serve": "npx serve ."
  },
  "notes": "No build step. index.html is a self-contained app; Leaflet, Turf.js and JSZip load from CDN. Deploy by serving the folder (e.g. GitHub Pages)."
}
